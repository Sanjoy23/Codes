#include <bits/stdc++.h>
using namespace std;


int main()
{
    int tc;
    cin >> tc;
    int n, d;
    while(tc--)
    {
        cin >> n >> d;
        int a[n+4],b[n];
        for(int i=0; i<n; i++)
        {
            cin >> a[i];
        }
        int ans = 0, check,temp;
        for(int i=0; i<n; i++)
        {
            if(a[i]> d)
            {
                temp = a[i]
                for(int j=0; i!=j,j<n; j++)
                {
                    if(a)
                }
            }
        }
    }

}
