#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc;
    cin >> tc;
    while(tc--){
        int n, m;
        cin >> n >> m;
        if(n % m == 0)cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
    }

}
