#include <bits/stdc++.h>
using namespace std;

struct node{
    int data;
    node *next;

};

struct node *head;

int createLinkedlist(node *head, int data){


}

int insertion(node *head, int data){

    node *newNode = new node;
    newNode->next = NULL;
    newNode->data = data;
        if(head == NULL){
            return head = newNode;
        }


}

int main(){


}
